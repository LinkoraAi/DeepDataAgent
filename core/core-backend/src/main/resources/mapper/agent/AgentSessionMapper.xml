<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkroa.deepdataagent.agent.infrastructure.persistence.mapper.AgentSessionMapper">

    <select id="selectActiveSessions" resultType="com.linkroa.deepdataagent.agent.infrastructure.persistence.entity.AgentSessionEntity">
        SELECT *
        FROM agent_session
        WHERE status = 'active'
          AND is_deleted = 0
        ORDER BY COALESCE(last_message_at, created_at) DESC
    </select>

</mapper>
