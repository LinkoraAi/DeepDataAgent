<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkroa.deepdataagent.agent.infrastructure.persistence.mapper.DialogueMapper">

    <select id="selectBySessionId" resultType="com.linkroa.deepdataagent.agent.infrastructure.persistence.entity.DialogueEntity">
        SELECT *
        FROM dialogue
        WHERE session_id = #{sessionId}
        ORDER BY id ASC
    </select>

    <select id="selectRoundsBySessionId" resultType="com.linkroa.deepdataagent.agent.infrastructure.persistence.entity.DialogueEntity">
        SELECT *
        FROM dialogue
        WHERE session_id = #{sessionId} AND is_deleted = 0
        <if test="beforeDialogueId != null">
            AND id &lt; #{beforeDialogueId}
        </if>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <select id="selectRunning" resultType="com.linkroa.deepdataagent.agent.infrastructure.persistence.entity.DialogueEntity">
        SELECT *
        FROM dialogue
        WHERE status = 'RUNNING' AND is_deleted = 0
    </select>

    <select id="selectMessages" resultType="java.lang.String">
        SELECT messages
        FROM dialogue
        WHERE id = #{id}
    </select>

    <update id="updateMessages">
        UPDATE dialogue
        SET messages = CAST(#{messages} AS jsonb)
        WHERE id = #{id}
    </update>

    <update id="updateMessagesAndStatus">
        UPDATE dialogue
        SET status = #{status},
            messages = CAST(#{messages} AS jsonb),
            end_time = now()
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE dialogue
        SET status = #{status},
            end_time = now()
        WHERE id = #{id}
    </update>

    <update id="updateStatusAndMetadata">
        UPDATE dialogue
        SET status = #{status},
            metadata = CAST(#{metadata} AS jsonb),
            end_time = now()
        WHERE id = #{id}
    </update>

    <update id="markAllRunningAsFailed">
        UPDATE dialogue
        SET status = 'FAILED',
            end_time = now()
        WHERE status = 'RUNNING' AND is_deleted = 0
    </update>

</mapper>
