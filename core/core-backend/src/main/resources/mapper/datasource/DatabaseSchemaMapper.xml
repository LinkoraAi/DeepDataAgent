<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkroa.deepdataagent.datasource.infrastructure.persistence.mapper.DatabaseSchemaMapper">

    <select id="selectByConnectionId" resultType="com.linkroa.deepdataagent.datasource.infrastructure.persistence.entity.DatabaseSchemaEntity">
        SELECT *
        FROM database_schema
        WHERE connection_id = #{connectionId}
          AND is_deleted = 0
        ORDER BY schema_name ASC
    </select>

    <delete id="deleteByConnectionId">
        DELETE FROM database_schema
        WHERE connection_id = #{connectionId}
    </delete>

    <update id="softDeleteByConnectionId">
        UPDATE database_schema
        SET is_deleted = 1,
            updated_at = datetime('now')
        WHERE connection_id = #{connectionId}
    </update>

    <select id="selectByConnectionIdAndSchemaName" resultType="com.linkroa.deepdataagent.datasource.infrastructure.persistence.entity.DatabaseSchemaEntity">
        SELECT *
        FROM database_schema
        WHERE connection_id = #{connectionId}
          AND schema_name = #{schemaName}
          AND is_deleted = 0
        LIMIT 1
    </select>

</mapper>
