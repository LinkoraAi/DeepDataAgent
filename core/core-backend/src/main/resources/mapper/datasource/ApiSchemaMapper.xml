<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkroa.deepdataagent.datasource.infrastructure.persistence.mapper.ApiSchemaMapper">

    <select id="selectByConnectionIdAndName" resultType="com.linkroa.deepdataagent.datasource.infrastructure.persistence.entity.ApiSchemaEntity">
        SELECT *
        FROM api_schema
        WHERE connection_id = #{connectionId}
          AND name = #{name}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="selectByConnectionId" resultType="com.linkroa.deepdataagent.datasource.infrastructure.persistence.entity.ApiSchemaEntity">
        SELECT *
        FROM api_schema
        WHERE connection_id = #{connectionId}
          AND is_deleted = 0
        ORDER BY name ASC
    </select>

    <update id="deleteByConnectionId">
        UPDATE api_schema
        SET is_deleted = 1,
            updated_at = datetime('now')
        WHERE connection_id = #{connectionId}
    </update>

</mapper>
