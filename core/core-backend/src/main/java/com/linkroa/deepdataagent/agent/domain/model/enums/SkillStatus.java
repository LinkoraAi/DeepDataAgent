package com.linkroa.deepdataagent.agent.domain.model.enums;

/**
 * 技能状态（预留 CHECKING / REJECTED）
 */
public enum SkillStatus {

    /** 可用 */
    ACTIVE,
    /** 审核中（预留） */
    CHECKING,
    /** 已驳回（预留） */
    REJECTED
}